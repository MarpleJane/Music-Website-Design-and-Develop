require(["Vue", "VueRouter", "MyComponents"], function(Vue, VueRouter, MyC) {
	Vue.use(VueRouter);
	// var Nav = { template: MyC.Nav };

	var routes = [
	  // { path: "/login", component: Login },
	  { path: "/signup", component: SignUp },
	]

})